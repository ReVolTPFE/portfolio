services:
  nuxt:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: nuxt-portfolio
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - HOST=0.0.0.0
    command: npm run dev
